{
  "name": "@mainframe/rpc-stream",
  "version": "0.2.2",
  "description": "Stream-based RPC",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "files": ["lib/*"],
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test": "flow check",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@mainframe/rpc-base": "^0.1.0",
    "@mainframe/rpc-error": "^0.1.0"
  },
  "peerDependencies": {
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.73.0",
    "rxjs": "^6.0.0"
  }
}
