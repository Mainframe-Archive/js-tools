{
  "name": "@mainframe/rpc-web3",
  "version": "0.1.0",
  "description": "Web3 RPC",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "files": ["lib/*"],
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test": "flow check",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@mainframe/rpc-request": "^0.1.0",
    "@mainframe/transport-web3": "^0.1.0"
  },
  "devDependencies": {
    "flow-bin": "^0.69.0"
  }
}
