{
  "name": "@mainframe/rpc-browser",
  "version": "0.1.0",
  "description": "RPC for browser",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@mainframe/rpc-http-browser": "^0.1.0",
    "@mainframe/rpc-web3": "^0.1.0",
    "@mainframe/rpc-ws-browser": "^0.1.0"
  }
}
