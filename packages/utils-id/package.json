{
  "name": "@mainframe/utils-id",
  "version": "0.1.0",
  "description": "Unique identifier utilities",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test:types": "flow check",
    "test:unit": "jest",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "nanoid": "^1.0.3"
  },
  "devDependencies": {
    "flow-bin": "^0.75.0"
  }
}