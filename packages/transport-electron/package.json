{
  "name": "@mainframe/transport-electron",
  "version": "0.1.0",
  "description": "Electron transport",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "clean": "del lib",
    "build:js": "BABEL_ENV=electron-renderer babel src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test:types": "flow check",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0"
  },
  "peerDependencies": {
    "electron": "^3.0.0",
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "electron": "^3.0.2",
    "flow-bin": "^0.82.0",
    "rxjs": "^6.2.2"
  }
}
