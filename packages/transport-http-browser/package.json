{
  "name": "@mainframe/transport-http-browser",
  "version": "0.1.0",
  "description": "HTTP transport for browser",
  "repository": "git@github.com:MainframeHQ/js-tools.git",
  "main": "lib/index.js",
  "author": "Mainframe",
  "license": "MIT",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test:types": "flow check",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@mainframe/transport-create-http": "^0.1.0"
  },
  "devDependencies": {
    "flow-bin": "^0.74.0"
  }
}
